# Contributing

Use Rust stable, format with `cargo fmt`, lint with `cargo clippy --workspace --all-targets -- -D warnings`, and add tests for state transitions or HTTP behavior you change. Keep provider/model logic outside the core. Do not commit secrets, private prompts, or artifacts.
