docs: badges, working doc links, and no orphaned wiki directory

The front page had no badges at all, which is the fastest way for a visitor to
read a repository as unmaintained: nothing showed that the build passes, what
the licence is, or that a container image exists. Adds CI, container, image,
licence and free-tier badges.

The documentation table used an empty header row, which GitHub renders as a
blank strip above the table - it looked broken rather than minimal.

wiki/ held three pages while the repository's GitHub wiki is disabled, so they
were unreachable and the README linked into a directory a visitor would not
think to open. Moved to docs/ alongside everything else, and linked properly.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01EXZMXYug9ti9L5qcB494zp